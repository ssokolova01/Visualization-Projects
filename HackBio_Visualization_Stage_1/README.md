# Stage 1 - Functions, Loops and Input Validation in R #

Two command-line tools written in base R: a GC content calculator and a protein molecular weight calculator. The focus of this stage is writing functions that handle real input robustly — mixed case, invalid characters, sensible defaults — rather than assuming clean data.
